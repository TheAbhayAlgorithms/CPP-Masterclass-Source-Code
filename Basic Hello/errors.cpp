/*
1. Compile Time Error - occurs basically during the compilation of the code
    Programming langauges have some basic rules that needs to be followed, and if not so, code will not be compiled at the end, and you'll get several errors.

    You're not going to get your binary executable code
    and when you have this, you have to go back and fix it so as to compile.

2. Runtime Errors - this will not cause compilation, code is going to compile, but when the
    compilation comletes, its not going to do what you want, like if u want green on some user interface, it shows grey.
l   ogical error that u have in your program and needed to be fixed to get rid of this.
    sometimes it can also crash the program completely.

*/

