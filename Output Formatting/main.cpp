#include<iostream>
using namespace std;
int main() {
    // https://en.cppreference.com/cpp/io/manip
    // prefer to the link for more info.




    // flush is used to send the data directly to the terminal, actually what happens is that data doesn't goes directly to the terminal instead it first goes to buffer(a temporary space) and then goes to terminal

}